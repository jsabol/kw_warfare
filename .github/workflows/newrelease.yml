# This is a basic workflow to help you get started with Actions
name: Release

# Controls when the action will run.
on:
  workflow_dispatch:
    inputs:
      new_version:
        description: 'Version to release'
        required: true


env:
  MODULE_NAME: kw_warfare

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Checkout
        id: checkout
        uses: actions/checkout@v2
        with:
          # Fetch all versions for tag/changelog generation
          fetch-depth: 0

      - name: Calculate Version
        id: calculate_version
        uses: mathieudutour/github-tag-action@v4.5
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          dry_run: true

      - name: Update Module
        id: update_module
        env:
          NEW_VERSION: ${{ github.event.inputs.new_version }}
          NEW_TAG: v${{github.event.inputs.new_version}}
        run: |
          echo "${NEW_VERSION}"
          echo "${NEW_TAG}"
          jq '. + {"version": env.NEW_VERSION, "download": (env.GITHUB_SERVER_URL + "/" + env.GITHUB_REPOSITORY + "/releases/download/" + env.NEW_TAG + "/" + env.MODULE_NAME + "-" + env.NEW_TAG + ".zip")}' module.json > _module.json
          mv _module.json module.json

      - name: Update Changelog
        id: update_changelog
        env:
          CHANGELOG: ${{ steps.calculate_version.outputs.changelog }}
        run: |
          mv CHANGELOG.md _CHANGELOG.md || touch _CHANGELOG.md
          echo "${CHANGELOG}" > CHANGELOG.md
          cat _CHANGELOG.md >> CHANGELOG.md
          rm -f _CHANGELOG.md

      - name: Commit Changes
        id: commit_changes
        uses: EndBug/add-and-commit@v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          add: module.json CHANGELOG.md
          message: |
            chore(build): Releasing v${{github.event.inputs.new_version}}

      - name: Commit Tag
        id: commit_tag
        uses: mathieudutour/github-tag-action@v4.5
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{github.event.inputs.new_version}}
          release_name: Release v${{github.event.inputs.new_version}}
          body: |
            Changes in this release:
            ${{ steps.calculate_version.outputs.changelog }}
          draft: false
          prerelease: false

      - name: Build Zip
        id: build_zip
        env:
          NEW_TAG: v${{github.event.inputs.new_version}}
        run: |
          zip -r /tmp/module.zip . -x .git\*

      - name: Upload Release Asset
        id: upload_release_asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: /tmp/module.zip
          asset_name: ${{ env.MODULE_NAME }}-v${{github.event.inputs.new_version}}.zip
          asset_content_type: application/zip
